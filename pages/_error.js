const { default: Layout } = require("../components/Layout");

import Layout from "../components/Layout";

export default () => (
  <Layout title="Error!!!">
    <p>해당 페이지를 가져올 수 없습니다. 죄송합니다.</p>
  </Layout>
);
